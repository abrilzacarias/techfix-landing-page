---
// Aquí puedes definir cualquier lógica de Astro si es necesario, pero en este caso no es requerido.
---

<header class="fixed w-full bg-white/90 dark:bg-secondary-950/90 backdrop-blur-xs z-50 py-4 transition-colors duration-300">
  <div class="container-custom flex items-center justify-between">
    <a class="flex items-center">
      <img src="/src/assets/techfixlogo.png" alt="Techfix logo" class="h-10 w-auto" />
      <span class="ml-2 text-xl font-display font-semibold text-secondary-900 dark:text-white">Techfix</span>
    </a>
    
    <nav class="hidden md:flex items-center space-x-8">
      <a href="/" class="nav-link">Inicio</a>
      <a href="/features" class="nav-link">Funcionalidades</a>
      <a href="/pricing" class="nav-link">Precios</a>
      <a href="/contact" class="nav-link">Contacto</a>
    </nav>

    <div class="flex items-center space-x-4">
      <button class="p-2 rounded-full text-secondary-500 hover:text-secondary-700 dark:text-secondary-200 dark:hover:text-primary-500 focus:outline-hidden focus:ring-2 focus:ring-primary-500">
        <!-- Iconos para el modo oscuro -->
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0A8.646 3.646"></path>
        </svg>
      </button>
      <a href="#signup" class="hidden sm:inline-flex btn-primary">Comienza ahora</a>
      
      <!-- Mobile Menu Button and Dropdown -->
      <div x-data="{ open: false }">
        <button 
          @click="open = !open"
          class="md:hidden p-2 rounded-md text-secondary-600 hover:text-secondary-900 dark:text-secondary-300 dark:hover:text-white focus:outline-hidden focus:ring-2 focus:ring-primary-500"
          aria-label="Toggle menu"
        >
          <svg x-show="!open" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg x-show="open" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>

        <div x-show="open" class="absolute top-16 right-4 w-48 py-2 bg-white dark:bg-secondary-900 rounded-md shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-hidden">
          <a href="/features" class="dropdown-link">Funcionalidades</a>
          <a href="/pricing" class="dropdown-link">Precios</a>
          <a href="/contact" class="dropdown-link">Contacto</a>
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  /* Estilo general del header */
  header a {
    transition: transform 0.3s ease, color 0.3s ease;
  }

  /* Estilo para los enlaces de navegación */
  .nav-link {
    text-decoration: none;
    color: #4a5568; /* color de texto por defecto */
    font-weight: medium;
    transition: transform 0.3s ease, color 0.3s ease;
  }

  .nav-link:hover {
    color: #3182ce; /* Color de texto en hover */
    transform: scale(1.05); /* Agranda el enlace en hover */
  }

  .nav-link:active {
    transform: scale(1.1); /* Agranda el enlace al hacer clic */
    color: #48bb78; /* Cambia el color al hacer clic */
  }

  /* Estilo para los enlaces del menú desplegable móvil */
  .dropdown-link {
    display: block;
    padding: 10px 20px;
    color: #4a5568;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .dropdown-link:hover {
    background-color: #f7fafc; /* Fondo de hover */
    color: #3182ce; /* Cambia el color del texto en hover */
  }

  /* Anima el menú móvil con transiciones */
  .x-show {
    opacity: 0;
    transform: scale(0.95);
    transition: opacity 0.3s ease, transform 0.3s ease;
  }

  .x-show[x-show="open"] {
    opacity: 1;
    transform: scale(1);
  }

  /* Estilo del botón de modo oscuro */
  .dark\:fill-slate-800 { fill: #1e293b; }
  .dark\:fill-slate-600 { fill: #475569; }
  .dark\:fill-slate-500 { fill: #64748b; }
  .dark\:fill-slate-700 { fill: #334155; }
  .dark\:stroke-slate-300 { stroke: #cbd5e1; }
  .dark\:stroke-teal-500 { stroke: #14b8a6; }
  .dark\:fill-white { fill: #ffffff; }

  /* Cambios para el tema oscuro */
  @media (prefers-color-scheme: dark) {
    .dark\:fill-slate-800 { fill: #1e293b; }
  }
</style>
